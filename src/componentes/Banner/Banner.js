import './Banner.css'

export const Banner = () => {
    //JSX parte visual do react, como ele lê e transforma o elemento no DOM
    return (
        <header className = "banner">
            <img src= "/imagens/banner-skillPet.png" alt= "banner principal da página"/>
        </header>
    )
}

